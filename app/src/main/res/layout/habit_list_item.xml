<?xml version="1.0" encoding="utf-8"?><!-- A CardView that contains a TextView -->
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cv_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"

    android:clickable="true"
    android:focusable="true"
    android:foreground="?selectableItemBackground"
    app:cardCornerRadius="8dp"
    app:cardPreventCornerOverlap="false"
    app:contentPadding="@dimen/padding_medium">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/list_item_habit_title"
            style="@style/ListItemTitle"
            tools:text="Read 10 minutes"
            tools:textColor="@color/text_primary" />

        <TextView
            android:id="@+id/list_item_reset_period"
            style="@style/ListItemResetFreq"
            tools:text="today" />

        <TextView
            android:id="@+id/list_item_count"
            style="@style/ListItemScore"
            tools:text="00" />

    </LinearLayout>

</android.support.v7.widget.CardView>